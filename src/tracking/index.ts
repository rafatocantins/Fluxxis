/**
 * Behavior Tracking
 */

// Re-export from hooks for convenience
export { useBehaviorObserver } from '../hooks/useBehaviorObserver';
export type { BehaviorObserverOptions, BehaviorMetrics } from '../hooks/useBehaviorObserver';
